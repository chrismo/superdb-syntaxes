// mmm ... pi
const pi=3.14

// this is a single line comment
// TODO: gotta to do stuff (no TODO highlighting for free here? bummer)
op myop(): (
  over this
  | s1:='single yield string',s2:="double-quote string"
  | dur:=duration("3d")
  | yield {a:int64("5"),b:max(uint64(34)),c:{d:|"a":12|}}
)

func kcast(): (
  uint8(2)
)
